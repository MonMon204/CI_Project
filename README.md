# CI_Project
A fully modular neural network library built from scratch using NumPy. Includes custom layers, activations, loss functions, optimizers, a Sequential model class, gradient checking tools, and example notebooks (XOR + MNIST Autoencoder + SVM classification). Designed for learning core deep‑learning fundamentals without using high‑level frameworks.
